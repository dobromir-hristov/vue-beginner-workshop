{
  "name": "vue-workshop",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "styles": "stylus assets/styles/style.styl",
    "styles:watch": "npm run styles -- -w",
    "build": "npm run stylus && cd 7-cli-sfc/solition && npm run build && cp _redirects /dist"
  },
  "author": "Dobromir Hristov <dobri@digital-cult.com> (http://digital-cult.com/)",
  "license": "MIT",
  "dependencies": {
    "stylus": "^0.54.5"
  }
}
